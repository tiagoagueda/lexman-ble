name: CI

on:
  push:
    branches:
      - main
  pull_request:

concurrency:
  group: ${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4.2.2
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: pre-commit/action@v3.0.1

  # Make sure commit messages follow the conventional commits convention:
  # https://www.conventionalcommits.org
  commitlint:
    name: Lint Commit Messages
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4.2.2
        with:
          fetch-depth: 0
      - uses: wagoid/commitlint-github-action@v4.1.11

  hacs:
    runs-on: "ubuntu-latest"
    name: HACS
    steps:
      - name: Check out the repository
        uses: "actions/checkout@v4.2.2"

      - name: HACS validation
        uses: "hacs/action@22.5.0"
        with:
          category: "integration"
          ignore: brands

  hassfest:
    runs-on: "ubuntu-latest"
    name: Hassfest
    steps:
      - name: Check out the repository
        uses: "actions/checkout@v4.2.2"

      - name: Hassfest validation
        uses: "home-assistant/actions/hassfest@master"

  # test:
  #   strategy:
  #     fail-fast: false
  #     matrix:
  #       python-version:
  #         - "3.9"
  #         - "3.10"
  #         - "3.11"
  #         - "3.11"
  #         - "3.12"
  #         - "3.13"
  #       os:
  #         - ubuntu-latest
  #   runs-on: ${{ matrix.os }}
  #   steps:
  #     - uses: actions/checkout@v4.2.2
  #     - name: Set up Python
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: ${{ matrix.python-version }}
  #     - uses: snok/install-poetry@v1
  #     - name: Install Dependencies
  #       run: poetry install
  #     - name: Test with Pytest
  #       run: poetry run pytest --cov-report=xml
  #     - name: Upload coverage to Codecov
  #       uses: codecov/codecov-action@v3
  #       with:
  #         token: ${{ secrets.CODECOV_TOKEN }}

  # NOTE: The PyPI semantic-release job was removed. The BLE library is now
  # vendored under custom_components/lexman_ble/lexman_ble and this fork is not
  # published to PyPI. Bump custom_components/lexman_ble/manifest.json `version`
  # by hand when you want HACS to offer an update.
